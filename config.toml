# 所有周和 Session 共用的 Ollama 配置。
# 地址使用 Ollama 默认端口 11434；如服务器使用其他端口，在这里修改。
[ollama]
host = "http://192.168.31.98:11434"

# 默认使用 14b；需要时可改为 "qwen2.5:3b"。
model = "qwen2.5:14b"

# 单次请求超时，单位：秒。
timeout = 300
