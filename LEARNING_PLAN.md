# AI Agent 开发学习计划

采用 Python，现扩展为 **13 个主题模块、65 个 Session**。保留 week_01～week_13 目录以保持已有链接；模块不再等于一个自然周。每节约 2～4 小时，可按每周 6～8 小时分次学习，实际时长以实验和验收为准，不再承诺 13 周学完。

2026-09-21 全课程审查发现基础、协议、实践和生产能力缺口，已增加 26 节独立教案。完整性依据是[知识覆盖与验收矩阵](CURRICULUM_AUDIT.md)，不是文件数量。

学习目标：独立开发一个能查询资料、调用工具、管理状态、使用 Skill、处理失败，并能被评估和部署的 Agent。

## 学习方式与项目约定

- 遵循 [AGENTS.md](AGENTS.md)：每节课先读取已保存教案，再开始讲解；缺失或需要修正时局部补充；当前节和继续学习的起点记录在 [LEARNING_PROGRESS.md](LEARNING_PROGRESS.md)。

- 按 `week_01/session_01/` 组织学习内容；65 节教案已预先准备，按学习进度取用，材料生成不代表学习完成。
- 各 Session 通过 `common.llm.create_llm_client()` 统一调用模型；公共模块从根目录 [config.toml](config.toml) 读取地址、模型和超时。采样等实验变量在各实验中单独设置和展示。
- 每个 Session 按“理解机制 → 最小实现 → 对照或故障验证”推进，每模块第 3 节做阶段复盘，最后一节做模块复盘。
- 每个 Session 保留学习笔记、示例代码（适用时）、问题与结论；实验回答和统计默认只打印到控制台，不自动写入结果文件。
- Python 基础、类型标注、HTTP、JSON、异常处理、Git 和 uv 为基础；异步、数据校验和测试工具在对应实验中补齐。
- 前期直接调用模型并手写执行循环，第 8 周再引入 LangGraph，对照理解框架承担的职责。
- 从第 2 周开始积累固定测试样本；权限检查从第 3 周工具调用开始落实。
- 已有材料入口见根 README，当前进度见 LEARNING_PROGRESS.md；教案或代码存在不代表实验已执行或课程已完成。下面的验收项由实际结果确认后勾选。

## 贯穿项目：知识库客服助手

使用模拟订单、模拟客户和本地测试数据，逐步实现：

1. 理解用户意图，识别缺失信息并追问。
2. 检索产品和售后资料，给出有来源的回答。
3. 查询订单，生成处理建议，并在需要时请求人工审核。
4. 创建模拟工单，查询实际结果，记录执行状态。
5. 支持会话恢复、Skill 复用、权限控制和自动评估。

练习中的身份、权限和审批由程序验证。模型提出的动作需要经过执行器检查；审批后的参数发生变化时需要重新核对授权。

完整课件入口见[课程总目录](README.md#课程总目录)。原有后续课与新增 26 节提供教案与文内示例，独立脚本按实际学习需要实现。

## 课程总览

| 周次 | 主题 | 本周产出 |
| --- | --- | --- |
| [第 01 周](#week-01) | LLM、Agent 与 Prompt 基础 | 温度实验、架构分析、Prompt 对比 |
| [第 02 周](#week-02) | 模型能力验证与可靠调用 | 模型能力报告、结构化意图分类器 |
| [第 03 周](#week-03) | 工具调用与数据契约 | 带校验和权限控制的工具执行器 |
| [第 04 周](#week-04) | 手写 Agent 循环与任务状态 | 能追问、取消和验证结果的命令行 Agent |
| [第 05 周](#week-05) | Agent Skills | Skill、加载器和触发评估记录 |
| [第 06 周](#week-06) | RAG 与知识数据管理 | 可引用来源的知识库问答助手 |
| [第 07 周](#week-07) | 上下文编排与记忆 | 上下文策略、可纠正和隔离的记忆存储 |
| [第 08 周](#week-08) | LangGraph、持久化与人工介入 | 可暂停、恢复且避免重复写入的执行流程 |
| [第 09 周](#week-09) | 工作流、规划与结果验证 | 固定工作流与动态规划的对比实验 |
| [第 10 周](#week-10) | 评估、追踪与实验管理 | 分层评估脚本、调用轨迹和对比报告 |
| [第 11 周](#week-11) | MCP 与安全边界 | MCP 服务、客户端接入及安全测试 |
| [第 12 周](#week-12) | 服务化与运行可靠性 | 可部署、可取消、可观测的 Agent 服务 |
| [第 13 周](#week-13) | 综合项目与验收 | 项目交付、评估报告、演示与复盘 |

<a id="week-01"></a>

## 第 01 周：LLM、Agent 与 Prompt 基础

目标：建立准确的基础概念，学会用实验观察模型行为。

| Session | 学习内容 | 实践 |
| --- | --- | --- |
| 01：模型与采样 | Token、上下文、Temperature、Top-p；训练与推理；模型、推理服务、客户端的区别；量化与资源需求入门 | 用 `qwen2.5:14b` 对同一 prompt，在 0、0.5、1.0 温度下各运行 3 次；记录模型及运行条件 |
| 02：架构与适用边界 | 单次模型调用、固定 Workflow、Agent 的区别；目标、工具、环境反馈、终止条件；认识 Skill、RAG、记忆与微调的用途 | 对客服分类、固定工单流程、多步调研分别画出执行过程，说明由代码还是模型决定下一步 |
| 03：Prompt 与基础验证 | 角色、任务、约束、输出格式；Few-shot；JSON 语法正确与任务正确的区别 | 对相同测试输入比较普通、结构化、Few-shot 三版 Prompt，用 Python 检查 JSON 和分类结果 |
| 04：[Python 工程基础与测试入口](week_01/session_04/README.md) | 能独立阅读契约、定位异常，并用可替换依赖测试一个业务函数。 | 离线正常/失败机制 → 本节扩展实验 → 验收；具体步骤见教案 |
| 05：[模型机制、采样与能力边界](week_01/session_05/README.md) | 建立 tokenizer、注意力、自回归、训练、推理和资源之间的因果关系。 | 离线正常/失败机制 → 本节扩展实验 → 验收；具体步骤见教案 |

验收：

- [ ] 观察控制台中三组温度实验的真实输出，说明文字一致性、语义正确性和任务成功的区别；不把 3 次样本当作普遍规律。
- [ ] 能解释简单意图分类为何不一定需要 Agent，工具能力、执行权限分别由谁提供。
- [ ] 三版 Prompt 使用相同输入比较，保留错误样本和结果，而非只展示成功案例。

已有入口：[第一周笔记](week_01/week1-llm-agent-basics.md)、[Session 01 运行说明](week_01/session_01/README.md)、[温度实验代码](week_01/session_01/main.py)、[Session 02 讲解与练习](week_01/session_02/README.md)、[Session 03 讲解与实验](week_01/session_03/README.md)。Session 01 测试 Temperature，Top-p 对比留作可选扩展。

本模块共 **5 节**；新增课的目标、前置、完整示例与验收已保存。原前三节验收继续有效；新增能力按各节清单补验，不默认通过。

<a id="week-02"></a>

## 第 02 周：模型能力验证与可靠调用

目标：明确当前模型和接口能做什么，得到可校验、可诊断的响应。

已有入口：[第二周概览](week_02/README.md)、[Session 01 讲义与实验](week_02/session_01/README.md)、[Session 02 教案与练习](week_02/session_02/README.md)、[Session 03 教案与基线实验](week_02/session_03/README.md)。

| Session | 学习内容 | 实践 |
| --- | --- | --- |
| 01：能力预检与请求协议 | 模型能力、服务端 API、客户端实现的边界；消息角色；流式与非流式响应；上下文和输出限制 | 用固定样本检查中文指令、结构化输出、多轮保持；运行最小工具调用探针，正式执行器在第 3 周实现 |
| 02：结构化数据 | JSON Schema、Pydantic；必填项、枚举、类型；缺失信息；格式校验与业务校验 | 实现意图分类器，区分 JSON 无法解析、字段不合法和分类错误 |
| 03：可靠性与模型基线 | 超时、连接失败、空响应、截断、拒绝回答；延迟与资源记录；模型选型的质量和运行成本权衡 | 注入错误响应；建立初始开发集和保留测试集，产出模型能力报告 |
| 04：[服务商适配、能力契约与流式组装](week_02/session_04/README.md) | 知道换服务时哪些字段必须保留，并处理跨字节边界、终止事件与残缺响应。 | 离线正常/失败机制 → 本节扩展实验 → 验收；具体步骤见教案 |
| 05：[Prompt 进阶、指令与数据分离](week_02/session_05/README.md) | 把提示设计做成版本化实验，识别指令冲突、歧义与错误示例。 | 离线正常/失败机制 → 本节扩展实验 → 验收；具体步骤见教案 |
| 06：[多模态输入与交互事件基础](week_02/session_06/README.md) | 理解图片、OCR、语音转写怎样进入 Agent，并处理过期交互结果。 | 离线正常/失败机制 → 本节扩展实验 → 验收；具体步骤见教案 |

验收：

- [ ] 能明确记录“支持、失败、未验证”，不把接口名称相同当作能力一致。
- [ ] 流式响应先完整组装后校验；不完整响应不能作为成功结果。
- [ ] 故障可定位到模型输出、网络或客户端处理；后续优化使用开发集，保留测试集用于验收。

本模块共 **6 节**；新增课的目标、前置、完整示例与验收已保存。原前三节验收继续有效；新增能力按各节清单补验，不默认通过。

<a id="week-03"></a>

## 第 03 周：工具调用与数据契约

目标：模型提出工具调用，由程序校验、执行并回传结果。

| Session | 学习内容 | 实践 |
| --- | --- | --- |
| 01：调用协议 | 工具名称、描述、参数 Schema；调用与返回结果关联；单个及多个调用 | 实现订单查询和售后政策查询，回传结果后让模型生成回答 |
| 02：工具执行器 | 输入与返回值校验；业务约束；只读与写入分类；可信用户身份与权限 | 增加模拟工单创建工具，拒绝不存在的订单及越权操作 |
| 03：错误与选择 | 工具执行错误、协议错误、无结果、部分成功；分页、截断；相似工具选择；重试边界 | 混合订单查询、政策咨询和工单任务，测工具选择和参数正确率 |
| 04：[工具界面设计、检索选择与并发契约](week_03/session_04/README.md) | 把工具目录设计成模型能选择、执行器能约束、并发时能关联结果的接口。 | 离线正常/失败机制 → 本节扩展实验 → 验收；具体步骤见教案 |

验收：

- [ ] 参数即使是合法 JSON，仍必须通过业务和权限检查。
- [ ] 工具失败和无结果具有明确返回约定，模型不能将它们描述为已完成。
- [ ] 不直接执行模型生成的任意函数名或代码；通过已注册工具分发调用。

本模块共 **4 节**；新增课的目标、前置、完整示例与验收已保存。原前三节验收继续有效；新增能力按各节清单补验，不默认通过。

<a id="week-04"></a>

## 第 04 周：手写 Agent 循环与任务状态

目标：理解 Agent 运行时如何组织模型、工具、上下文、状态和用户输入。

| Session | 学习内容 | 实践 |
| --- | --- | --- |
| 01：最小执行循环 | 模型决策、工具执行、结果回传；循环停止；最大步数与调用预算 | 手写命令行 Agent，完成先查订单、再查政策的多步任务 |
| 02：任务与交互 | 目标、输入、约束、完成证据；缺信息追问；用户更正和取消；聊天记录与任务状态的区别 | 为任务分配标识，记录运行、等待用户、成功、失败、取消等状态 |
| 03：环境反馈与退出 | 模型回复与实际结果区分；重复动作检测；无进展退出；结构化执行记录 | 创建模拟工单后查询是否真实存在；故障或超预算时给出可解释的结束状态 |
| 04：[把真实模型接入手写循环](week_04/session_04/README.md) | 在同一个循环里替换决策器，保留工具权限、预算、观察和完成依据。 | 离线正常/失败机制 → 本节扩展实验 → 验收；具体步骤见教案 |

验收：

- [ ] 缺订单号时追问；用户更换订单后更新状态；取消后不再启动后续动作。
- [ ] “模型说完成”必须与工具结果或业务状态核对。
- [ ] 能从记录中重建工具调用顺序、参数、结果及停止原因，不依赖模型自述的推理过程。

本模块共 **4 节**；新增课的目标、前置、完整示例与验收已保存。原前三节验收继续有效；新增能力按各节清单补验，不默认通过。

<a id="week-05"></a>

## 第 05 周：Agent Skills

目标：编写可复用的任务方法，并让自己的 Python Agent 支持发现和按需加载。

| Session | 学习内容 | 实践 |
| --- | --- | --- |
| 01：编写 Skill | `SKILL.md`、名称与描述、适用场景、步骤、异常分支；`scripts/`、`references/`、`assets/` | 编写售后处理 Skill，包含政策说明和回复模板 |
| 02：发现与激活 | 元数据目录、显式选择、模型选择；渐进式加载；多个 Skill 的区分；脚本执行依赖 | 为第 4 周 Agent 增加加载器，先暴露名称和描述，再按需加载正文及资料 |
| 03：评估与维护 | 正确触发、误触发、漏触发；版本固定、依赖声明、更新回归和回退 | 对比无 Skill、旧版、新版 Skill；测试相似请求和不适用请求 |
| 04：[Skill 包验证、依赖与宿主生命周期](week_05/session_04/README.md) | 把可加载的文档包变成可审查、可回退的运行时资源，区分 Skill、插件、钩子和权限。 | 离线正常/失败机制 → 本节扩展实验 → 验收；具体步骤见教案 |

验收：

- [ ] Skill 的发现、内容加载和工具执行都有可检查的记录；仅放置文件不会被当作已实现集成。
- [ ] 能区分 Skill 的指导作用和执行器的权限职责，不因加载 Skill 自动放宽权限。
- [ ] 使用同一组案例比较效果，包含不应触发的案例，能回退到已有可用版本。

本模块共 **4 节**；新增课的目标、前置、完整示例与验收已保存。原前三节验收继续有效；新增能力按各节清单补验，不默认通过。

<a id="week-06"></a>

## 第 06 周：RAG 与知识数据管理

目标：从可信且有权限的资料中寻找证据，并据此回答。

详细技术清单、原遗漏项、深度与跨周映射见 [RAG 知识清单](week_06/RAG_ROADMAP.md)。第六周建立基础与核心对照，第七周做压缩/位置，第九周做 Agentic RAG，第十周做 RAGAS 与消融；RAPTOR/GraphRAG 增设 S06/S07 学习结构、来源、维护和对照实践；完整产品级工程仍需专项验证。S04/S05 分担真实索引和查询增强，不再挤进三节。

| Session | 学习内容 | 实践 |
| --- | --- | --- |
| 01：文档入库 | 解析/OCR/表格；固定、结构化、语义和父子分块；Embedding/索引基础；元数据/ACL；Hypothetical Questions | 建立可追溯知识库，比较条件保留、父子返回及问题索引 |
| 02：检索与回答 | BM25/向量混合检索、RRF；HyDE、Multi-Query、查询分解与 Step-Back；Cross-encoder 重排（BGE/Cohere 实现选型）；引用与无答案 | 比较召回、融合、重排及一种查询变换；区分真实模型与人工夹具 |
| 03：数据生命周期与架构 | 版本/删除/权限传播；RAPTOR 摘要树、GraphRAG 图与社区摘要的原理；Agentic RAG 概览；检索与生成评估 | 验证旧证据失效，设计派生索引维护实验；为第 9/10 周准备查询与评估样本 |
| 04：[解析质量、真实 Embedding 与向量索引](week_06/session_04/README.md) | 从可追溯文档构造索引，知道什么时候必须重新编码或重建。 | 离线正常/失败机制 → 本节扩展实验 → 验收；具体步骤见教案 |
| 05：[查询增强、重排与检索消融实验](week_06/session_05/README.md) | 在固定任务集上检验增强是否修复具体检索缺口，而不是一次打开所有策略。 | 离线正常/失败机制 → 本节扩展实验 → 验收；具体步骤见教案 |
| 06：[RAPTOR 分层索引与派生证据](week_06/session_06/README.md) | 通过构建摘要层理解跨片段问题，同时保留原文追溯和更新删除。 | 离线正常/失败机制 → 本节扩展实验 → 验收；具体步骤见教案 |
| 07：[GraphRAG、图检索与跨文档问题](week_06/session_07/README.md) | 理解实体关系、社区报告、局部与全局查询，避免把图的存在当作真实性。 | 离线正常/失败机制 → 本节扩展实验 → 验收；具体步骤见教案 |

验收：

- [ ] 答案可追溯到文档和片段；资料不足时说明缺口，不编造引用。
- [ ] 更新或删除内容后，旧内容不再作为有效证据；无权访问的文档不进入模型上下文。
- [ ] 能区分检索没找到、找到错误片段和生成回答出错。
- [ ] 能比较分块、BM25/向量、RRF/重排与查询变换的职责，用正常和失败样本说明取舍。
- [ ] 能区分 Hypothetical Questions 与 HyDE，说明 RAPTOR、GraphRAG、Agentic RAG 改变的环节。

本模块共 **7 节**；新增课的目标、前置、完整示例与验收已保存。原前三节验收继续有效；新增能力按各节清单补验，不默认通过。

<a id="week-07"></a>

## 第 07 周：上下文编排与记忆

目标：控制每轮提供给模型的信息，并管理可纠正、可删除、相互隔离的记忆。

| Session | 学习内容 | 实践 |
| --- | --- | --- |
| 01：上下文选择 | 指令、Skill、工具、证据和历史；Token 预算；RAG 抽取/生成压缩；文档重排列与 Lost-in-the-Middle | 比较事实保留、压缩遗漏及同一证据集合的位置影响 |
| 02：记忆存储 | 当前任务状态、会话历史、跨会话记忆的区别；结构化存储；来源、时间和作用域 | 用本地存储保存必要偏好，通过用户标识隔离读取 |
| 03：记忆质量 | 错误记忆纠正、冲突处理、过期与删除；避免把未经核实的模型输出变成事实 | 更新用户地址、删除偏好、制造冲突信息并验证后续行为 |
| 04：[记忆写入、检索与巩固评估](week_07/session_04/README.md) | 把长期记忆做成有来源、有用途、可纠正的闭环，而不是无限保存聊天。 | 离线正常/失败机制 → 本节扩展实验 → 验收；具体步骤见教案 |

验收：

- [ ] 缩小上下文后仍保留当前任务目标、订单和关键约束。
- [ ] 用户更正后不继续沿用旧值，不把另一个用户的信息带入当前任务。
- [ ] 能解释信息来自对话、记忆还是检索，说明摘要可能丢失哪些内容。

本模块共 **4 节**；新增课的目标、前置、完整示例与验收已保存。原前三节验收继续有效；新增能力按各节清单补验，不默认通过。

<a id="week-08"></a>

## 第 08 周：LangGraph、持久化与人工介入

目标：将已理解的循环迁移为可暂停、恢复的状态流程。

| Session | 学习内容 | 实践 |
| --- | --- | --- |
| 01：图与状态 | State、Node、Edge、条件分支；框架与手写循环的对应关系 | 迁移第 4 周 Agent，用同一测试集比较行为 |
| 02：暂停和恢复 | Checkpoint、持久化存储、人工批准/修改/拒绝；内存存储与进程重启的区别 | 在创建模拟工单前暂停，保存状态并在重启后继续 |
| 03：副作用与结果未知 | 重放和恢复边界；业务幂等；确认成功、确定失败、结果未知；恢复时查询核对 | 模拟“工单已创建，但响应丢失”，避免恢复后重复创建 |
| 04：[LangGraph 并行状态、子图与流式事件](week_08/session_04/README.md) | 理解 reducer、并行合并、子图边界和事件输出，防止迁移后丢状态。 | 离线正常/失败机制 → 本节扩展实验 → 验收；具体步骤见教案 |

验收：

- [ ] 重启后能从持久化状态恢复，拒绝审批后不会继续执行写操作。
- [ ] 审批对应具体动作和参数；参数改变时重新核对授权。
- [ ] 故障恢复后只有一张模拟工单；无法核对结果时标记为待确认，不能盲目重复写入。

本模块共 **4 节**；新增课的目标、前置、完整示例与验收已保存。原前三节验收继续有效；新增能力按各节清单补验，不默认通过。

<a id="week-09"></a>

## 第 09 周：工作流、规划与结果验证

目标：根据任务选择合适的执行方式，并用实际反馈检查计划。

| Session | 学习内容 | 实践 |
| --- | --- | --- |
| 01：固定工作流 | 串行、路由、并行；依赖关系；代码可确定的业务规则 | 实现分类后分流的客服流程，仅并行执行相互独立的查询 |
| 02：动态规划 | 任务拆解/重规划、预算、更正、Skill 复用；Agentic RAG 的查询分解、补检索和停止 | 处理目标变更，对比固定查询与根据证据缺口迭代检索 |
| 03：结果检查与有限修正 | 明确验收条件；检查工具及业务事实；有限次数修正；人工接管 | 对同一组任务比较固定流程与动态规划的成功率、调用数和耗时 |
| 04：[多 Agent 分工、交接与协作评估](week_09/session_04/README.md) | 能选择单 Agent、主管分工或交接模式，并把总预算、证据和责任传下去。 | 离线正常/失败机制 → 本节扩展实验 → 验收；具体步骤见教案 |
| 05：[A2A 与跨 Agent 任务协议](week_09/session_05/README.md) | 区分本进程分工与跨服务协作，识别发现、任务状态、产物及授权边界。 | 离线正常/失败机制 → 本节扩展实验 → 验收；具体步骤见教案 |

验收：

- [ ] 能解释哪些步骤由代码固定、哪些交给模型选择，并用实验支持取舍。
- [ ] 依赖前一步结果的动作按顺序执行；需求变化使旧计划失效时停止旧动作。
- [ ] 检查器有明确判断依据，不能仅靠模型自我评价无限循环。

本模块共 **5 节**；新增课的目标、前置、完整示例与验收已保存。原前三节验收继续有效；新增能力按各节清单补验，不默认通过。

<a id="week-10"></a>

## 第 10 周：评估、追踪与实验管理

目标：用可重复的证据判断系统是否改善，并定位错误发生的位置。

| Session | 学习内容 | 实践 |
| --- | --- | --- |
| 01：分层评估 | 格式、检索、工具/Skill、轨迹、终态；Precision/Recall/MRR/nDCG；RAGAS Context Precision/Recall、Faithfulness；代码/模型/人工评分 | 定义成功条件，演算指标并校准评分；SDK 适配见 S05，仍需实际运行与版本验证 |
| 02：追踪与复现 | 任务和步骤标识；请求、工具结果、状态变化；模型、Prompt、Skill、数据及参数版本 | 实现结构化日志和报告，保留必要信息并脱敏凭据 |
| 03：对照与回归 | 开发集和保留集、重复、负例和故障；RAG 组件消融；质量、延迟、调用量与成本 | 比较两个版本及 RAG 单项增强，保留退化和安全回归用例 |
| 04：[数据集设计、统计不确定性与评分器偏差](week_10/session_04/README.md) | 避免用少量挑选样本宣布改进，能设计可重复、分层且不泄漏的评价。 | 离线正常/失败机制 → 本节扩展实验 → 验收；具体步骤见教案 |
| 05：[追踪接入、线上反馈与 RAGAS 适配](week_10/session_05/README.md) | 把本地事件映射成可关联的观测数据，并将评分工具接到同一个模型边界。 | 离线正常/失败机制 → 本节扩展实验 → 验收；具体步骤见教案 |

验收：

- [ ] 能定位失败属于格式、检索、工具选择、执行还是业务判断；不只评价最终文字。
- [ ] 数据库或工具状态支持“已创建”等结论；报告保留部分成功和结果未知的情况。
- [ ] 记录重复运行的波动、测试样本和版本；本地推理记录耗时与资源，远程调用另记实际费用。

本模块共 **5 节**；新增课的目标、前置、完整示例与验收已保存。原前三节验收继续有效；新增能力按各节清单补验，不默认通过。

<a id="week-11"></a>

## 第 11 周：MCP 与安全边界

目标：理解标准化接入方式，并在接入外部内容和工具时落实访问控制。

| Session | 学习内容 | 实践 |
| --- | --- | --- |
| 01：MCP 架构与协议 | Host、Client、Server；Tools、Resources、Prompts；发现、调用和错误；本地与远程连接、版本兼容 | 跟踪一次完整调用，理解 MCP 与模型工具调用、Skill 的关系 |
| 02：实现与接入 | Python MCP 服务与客户端；从工具元数据到执行分发；认证与授权边界 | 将已有订单查询工具封装成 MCP 服务，让 Agent 和 Skill 复用 |
| 03：安全实验 | 提示注入；不可信文档、Skill、脚本和工具返回；身份与数据隔离；最小权限、执行限制和审计 | 在测试文档植入越权指令，测试跨用户查询、未经批准写入和凭据泄露 |
| 04：[远程 MCP、能力协商与授权](week_11/session_04/README.md) | 把本地 stdio 实验扩展到远程信任边界，理解认证、授权和版本兼容的不同职责。 | 离线正常/失败机制 → 本节扩展实验 → 验收；具体步骤见教案 |
| 05：[执行隔离、浏览器动作与威胁建模](week_11/session_05/README.md) | 给文件、代码、浏览器和网络工具建立可测试的边界，明确沙箱保护的范围。 | 离线正常/失败机制 → 本节扩展实验 → 验收；具体步骤见教案 |

验收：

- [ ] 能发现、调用工具并区分协议错误和工具执行错误；读取失败不被伪装成正常结果。
- [ ] 身份来自可信应用上下文，资源权限由程序判断，不由模型生成的用户标识决定。
- [ ] 注入内容不能改变程序的权限检查；日志不泄露凭据；记录尚未解决的风险和限制。

本模块共 **5 节**；新增课的目标、前置、完整示例与验收已保存。原前三节验收继续有效；新增能力按各节清单补验，不默认通过。

<a id="week-12"></a>

## 第 12 周：服务化与运行可靠性

目标：把 Agent 做成可使用、可排查、可维护的服务。

| Session | 学习内容 | 实践 |
| --- | --- | --- |
| 01：服务接口 | HTTP API、任务标识、状态查询、流式进度；长任务和普通请求的区别 | 用 FastAPI 提供提交、查询和取消接口，保留命令行入口用于排查 |
| 02：运行控制 | 异步、并发限制、超时、限流；取消传播；退避和有限重试；幂等与结果核对 | 注入模型超时、工具限流、客户端断开及重复提交，验证执行行为 |
| 03：部署与维护 | 环境变量和密钥、依赖版本、持久化、健康检查、日志；Prompt/Skill/流程版本发布与回退 | 在本地或容器中完成部署演练，运行回归并验证版本回退 |
| 04：[持久任务队列、租约与事务发件箱](week_12/session_04/README.md) | 理解进程崩溃和重复投递时如何接续任务，不丢状态也不重复业务写入。 | 离线正常/失败机制 → 本节扩展实验 → 验收；具体步骤见教案 |
| 05：[事件流、背压与人工审核体验](week_12/session_05/README.md) | 使客户端断线和慢消费时仍能理解任务状态，审批精确绑定动作。 | 离线正常/失败机制 → 本节扩展实验 → 验收；具体步骤见教案 |
| 06：[性能、路由、缓存与服务降级](week_12/session_06/README.md) | 在相同质量约束下测成本与延迟，不通过放宽授权换取速度。 | 离线正常/失败机制 → 本节扩展实验 → 验收；具体步骤见教案 |

验收：

- [ ] 多个任务和用户的状态不混用；取消后不启动后续步骤，并能核对已经发生的动作。
- [ ] 只对适合重试的操作重试，结果未知的写操作通过幂等或核对处理。
- [ ] 重启后有可解释的任务状态；更换版本前有回归结果，运行文档能指导重新启动服务。

本模块共 **6 节**；新增课的目标、前置、完整示例与验收已保存。原前三节验收继续有效；新增能力按各节清单补验，不默认通过。

<a id="week-13"></a>

## 第 13 周：综合项目与验收

目标：整合已学能力，交付有明确边界和证据的知识库客服助手。

| Session | 学习内容 | 实践 |
| --- | --- | --- |
| 01：需求与验收设计 | 明确支持的场景、用户身份、数据、可用工具、预算、审批和成功条件 | 完成需求说明和架构图，整理正常、缺信息、无答案、失败及越权案例 |
| 02：整合与故障修复 | 集成知识库、工具、Skill、记忆、持久化、人工审核与服务接口 | 完成端到端流程，修复高优先级问题并运行故障注入测试 |
| 03：评估与交付 | 在保留测试集上验收；对照基线；演示、文档、限制和后续改进 | 交付代码、运行说明、评估报告、代表性调用轨迹和学习复盘 |
| 04：[综合工程分层与端到端契约测试](week_13/session_04/README.md) | 把各节机制连成同一个可替换、可诊断的系统，验收真实边界而非函数数量。 | 离线正常/失败机制 → 本节扩展实验 → 验收；具体步骤见教案 |
| 05：[CI、发布、备份恢复与生产验收](week_13/session_05/README.md) | 使他人能重建环境、部署、回退和恢复，并用事先标准决定能否交付。 | 离线正常/失败机制 → 本节扩展实验 → 验收；具体步骤见教案 |
| 06：[模型适配、训练路线与课程总复盘](week_13/session_06/README.md) | 能判断何时使用 Prompt、RAG、工具、Skill 或训练，并把后续专项学习建立在现有证据上。 | 离线正常/失败机制 → 本节扩展实验 → 验收；具体步骤见教案 |

验收：

- [ ] 可完成“理解问题 → 补齐信息 → 查询证据 → 给出方案 → 必要审批 → 创建模拟工单 → 核验结果”的完整流程。
- [ ] 超时、重复请求、进程重启和取消有明确处理；不把未知结果误报为成功。
- [ ] 没有越权读取和未经授权的写操作；回答与实际状态、引用来源相符。
- [ ] 用事先确定的标准说明哪些场景通过、哪些失败；他人能按运行说明复现实验。

本模块共 **6 节**；新增课的目标、前置、完整示例与验收已保存。原前三节验收继续有效；新增能力按各节清单补验，不默认通过。

## 主线深度与专项深化

基础调用、工具/运行时、Skill、RAG/记忆、LangGraph、规划协作、评估、安全、服务与交付均已进入主线，不再用“以后选修”隐藏缺项。学习深度按下表明确；所有方向都需理解机制和失败边界，核心项目还需真实集成证据。

| 专项 | 主线已有课次 | 专项进一步实现 |
| --- | --- | --- |
| 多 Agent / A2A | W09 S04/S05 的分工、任务/产物和失败实验 | 跨组织发现、认证与生产级协作 |
| 浏览器 / 代码执行 | W11 S05 的动作核验、文件/进程/网络隔离 | 受限浏览器运行时、多网站适配及独立安全审计 |
| 多模态 / 语音 | W02 S06 的能力/输入/事件契约 | 指定视觉模型、OCR/ASR/TTS、实时设备与 WebRTC |
| 模型适配 / 训练 | W01 S05、W13 S06 的机制、数据与选型 | 实际 SFT/LoRA、DPO/RL 与训练资源工程 |
| RAG 进阶体系 | W06 S04～07；W09 S02；W10 S01/S05 | 大规模 ANN、完整 RAPTOR/GraphRAG 索引与更新服务 |
| 插件 / hooks / 中间件 | W05 S04 的宿主生命周期与依赖边界 | 特定平台打包、兼容矩阵和分发 |

不要求掌握每个同类框架、所有论文和每种数据库，但每个关键环节必须能解释、做实验、识别失败，并知道何时需要专项深入。以后发现新缺口先更新覆盖矩阵和对应教案，不把未来方法一概声称“已经学全”。

## 官方参考资料

以下资料与对应周次配合阅读。实现时按实际安装的模型、服务和库版本核对接口。

- 第 1、4、9 周：从简单调用、工作流到自主执行循环，参考 [Building effective agents](https://www.anthropic.com/engineering/building-effective-agents)。
- 第 1～3 周：采样参数、结构化输出及程序执行工具的职责，参考 [Ollama 参数说明](https://docs.ollama.com/modelfile)、[Structured Outputs](https://docs.ollama.com/capabilities/structured-outputs)、[Tool calling](https://docs.ollama.com/capabilities/tool-calling)。
- 第 5 周：文件结构、渐进加载和接入生命周期，参考 [Agent Skills 规范](https://agentskills.io/specification)、[接入指南](https://agentskills.io/client-implementation/adding-skills-support)。
- 第 7 周：每轮上下文选择与长期任务的信息组织，参考 [Effective context engineering](https://www.anthropic.com/engineering/effective-context-engineering-for-ai-agents)。
- 第 8 周：图状态、持久化与重新执行边界，参考 [LangGraph 概览](https://docs.langchain.com/oss/python/langgraph/overview)、[Persistence](https://docs.langchain.com/oss/python/langgraph/persistence)、[Time travel](https://docs.langchain.com/oss/python/langgraph/use-time-travel)。
- 第 10 周：任务、调用记录、真实结果和不同评分方法，参考 [Demystifying evals for AI agents](https://www.anthropic.com/engineering/demystifying-evals-for-ai-agents)。
- 第 11 周：应用、客户端、服务端及工具与资源的关系，参考 [MCP 架构](https://modelcontextprotocol.io/docs/learn/architecture)。

新增参考：各扩展课附固定 SDK/协议版本与官方来源；特别区分旧版 MCP 示例、新版规范和未运行的适配代码。
